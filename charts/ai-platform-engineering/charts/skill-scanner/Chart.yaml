apiVersion: v2
name: skill-scanner
description: |
  Standalone deployment of cisco-ai-defense/skill-scanner running its built-in
  REST API server. The CAIPE UI uploads zipped SKILL packages to /scan-upload
  for safety analysis. The service is unauthenticated and MUST stay
  cluster-internal (ClusterIP only — no Ingress).
type: application
version: 1.0.0-outshift.20260905.7 # Do NOT bump this. It will be updated automatically using the PR or release workflow
appVersion: "1.0.0-outshift.20260905.7" # Do NOT modify. It will be updated automatically using the release workflow
dependencies: []
